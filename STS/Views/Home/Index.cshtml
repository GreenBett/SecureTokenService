@{
    ViewData["Title"] = "Home Page";
}

<h1>Logged in User's Claims that are held in the cookie</h1>
<dl>
    @foreach (var claim in User.Claims)
    {
        <dt>@claim.Type</dt>
        <dd>@claim.Value</dd>

    }
</dl>
